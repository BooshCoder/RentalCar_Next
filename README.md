# Rental Car - Next.js + TypeScript

Верстка сайту для оренди автомобілів на основі дизайну з Figma, реалізована на Next.js 14 з TypeScript.

## 🚀 Технології

- **Next.js 14** - React фреймворк з App Router
- **TypeScript** - типізація коду
- **CSS Modules** - модульні стилі
- **React Hooks** - для управління станом

## 📁 Структура проекту

```
├── app/
│   ├── layout.tsx              # Root layout
│   ├── page.tsx                # Головна сторінка
│   ├── globals.css             # Глобальні стилі
│   ├── catalog/
│   │   └── page.tsx            # Сторінка каталогу
│   └── details/
│       └── [id]/
│           └── page.tsx        # Детальна сторінка (динамічна)
├── components/
│   ├── Header.tsx              # Компонент шапки
│   ├── CarCard.tsx             # Компонент картки автомобіля
│   ├── Filters.tsx             # Компонент фільтрів
│   └── BookingForm.tsx        # Форма бронювання
├── data/
│   └── cars.ts                 # Дані про автомобілі
├── types/
│   └── index.ts                # TypeScript типи
├── package.json
├── tsconfig.json
└── next.config.js
```

## 🛠️ Встановлення та запуск

### 1. Встановити залежності

```bash
npm install
```

### 2. Запустити dev сервер

```bash
npm run dev
```

Відкрийте [http://localhost:3000](http://localhost:3000) у браузері.

### 3. Збірка для продакшену

```bash
npm run build
npm start
```

## ✨ Функціональність

### Головна сторінка (`/`)
- Hero секція з закликом до дії
- Перехід на каталог

### Каталог (`/catalog`)
- ✅ Фільтрація за брендом, ціною, пробігом
- ✅ Динамічне відображення карток
- ✅ Load more для завантаження більше карток
- ✅ Heart buttons для додавання в обране (localStorage)
- ✅ Перехід на детальну сторінку

### Деталі (`/details/[id]`)
- ✅ Динамічне завантаження даних з URL
- ✅ Повна інформація про автомобіль
- ✅ Форма бронювання з валідацією
- ✅ Відображення умов оренди, характеристик, аксесуарів

## 🎨 Стилізація

- CSS Modules для компонентів
- Глобальні стилі в `app/globals.css`
- Адаптивний дизайн (mobile-first)
- Використання CSS Variables

## 📝 TypeScript

Проект повністю типізований:
- Типи для даних (`types/index.ts`)
- Типізація компонентів
- Типізація props та state

## 🔧 Особливості реалізації

- **App Router** - використання нової системи маршрутизації Next.js 14
- **Server Components** - для статичних сторінок
- **Client Components** - для інтерактивних елементів
- **Dynamic Routes** - для сторінки деталей (`[id]`)
- **Image Optimization** - використання Next.js Image компонента
- **Type Safety** - повна типізація з TypeScript

## 📦 Залежності

- `next` - Next.js фреймворк
- `react` - React бібліотека
- `react-dom` - React DOM
- `typescript` - TypeScript компілятор

## 🌐 Браузерна підтримка

- Chrome (останні версії)
- Firefox (останні версії)
- Safari (останні версії)
- Edge (останні версії)

## 📱 Адаптивність

Проект адаптивний для:
- Мобільних пристроїв (до 768px)
- Планшетів (769px - 1024px)
- Десктопів (1025px+)

## 🚀 Деплой

Проект готовий до деплою на:
- Vercel (рекомендовано)
- Netlify
- Інші платформи з підтримкою Next.js

```bash
# Для Vercel
vercel deploy
```

## 📄 Ліцензія

MIT
